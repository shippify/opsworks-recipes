{
  "client": {
    "name": "<%= node[:hostname] %>",
    "address": "<%= node[:ipaddress] %>",
    "subscriptions": [
      <% node[:sensu][:subscriptions].each do |subscription| -%>
      "<%= subscription %>"
      <% end %>
    ]
  }
}
